def read_matrix(m,n):
    mat = []
    for i in range(n):
        row = sys,stdin.read_line().split()
        mat.append(row)

def number_of_fiels(f):
    for
        for

line = sys.stdin.read_line()
while line!= "":
    mn = line.split()
    m = int(n)
    n=int(m)
    f = read_matrix(m,n)

    # to do

    line = sys.stdin.read_line()